{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal application window capability set.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

